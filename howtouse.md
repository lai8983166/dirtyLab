整使用流程（有 AutoDL 的情况）

整体顺序：配连接 → 建实验 → ComfyUI 出图 → 同步 → 评分 → AI 分析。

---

第 1 步：配置 AutoDL 连接（一次性）

1. 本地终端生成 SSH 密钥：  
   ssh-keygen -t ed25519 -f ~/.ssh/autodl_dirtylab -N ""  
   cat ~/.ssh/autodl_dirtylab.pub
2. 复制输出的公钥，粘到 AutoDL 控制台 → SSH 公钥管理 → 添加。
3. 启动你的 AutoDL 实例，记下分配的 host（类似  
   region-1.autodl.com）、port（数字）、username（一般是 root）。
4. 浏览器打开 http://127.0.0.1:5173 → 左侧导航 AutoDL 连接。
5. 填表：


    - 主机、端口、用户名
    - 私钥：cat ~/.ssh/autodl_dirtylab 的完整输出（包括 -----BEGIN ... PRIVATE KEY----- 头尾）
    - 远端实验根目录：通常是 /root/ComfyUI
    - 输入目录默认 input，前缀默认 ComfyUI_

6. 点 保存连接。
7. 点 运行测试。结果会分四阶段报：


    - SSH 连接失败 → 实例没启动 / 密钥不对 / 用户名错 / 端口不通
    - 远端根目录失败 → SSH 通了但路径不对（改成实际 ComfyUI 路径）
    - ComfyUI 输入/输出目录失败 → 路径下没有 input/ 或 output/，确认 ComfyUI 装在哪个目录


每个失败都会有具体提示，照着改。

---

第 2 步：创建实验

1. 导航 → 新建实验。
2. 填名称（必填）、目标/描述（可选，比如"修复人物右手"）。
3. 选一张本地图作为原图（PNG/JPG 都行）。
4. 点 创建实验，自动跳到实验详情页。  


详情页最上面有一行 你的远端工作目录，路径形如：  
 /root/ComfyUI/experiments/<一串ID>

把这个路径记下来——下一步要在 AutoDL 上操作。

---

第 3 步：在 ComfyUI 上跑出候选图

SSH 上 AutoDL：

ssh -i ~/.ssh/autodl_dirtylab root@<host> -p <port>

然后：

1. 创建对应目录：  
   mkdir -p /root/ComfyUI/experiments/<那串ID>/{input,output,masks,workflows}
2. 把原图传上去（也可以用 ComfyUI 的 web 上传）：

# 本地另开终端

scp -i ~/.ssh/autodl_dirtylab -P <port> 原图.png \  
 root@<host>:/root/ComfyUI/experiments/<那串ID>/input/original.png  
 3. 在 ComfyUI 里：  
 - Load Image 节点选 input/original.png  
 - 正常画 mask、调参、生成  
 - 每个想保留的输出都接一个 Save Image 节点（这是关键，平台只同步 Save Image 写到 output/  
 的文件）  
 - 如果想保留工作流：菜单 → 保存 → 放到 experiments/<那串ID>/workflows/ 下，或者用 ComfyUI  
 自带的 "save metadata next to image" 选项  
 4. 跑完几张后回到 dirtyLab。

---

第 4 步：同步候选图

1. 实验详情页 → 立即同步。
2. 等几秒，结果卡片会报：


    - 快照 #1 · 成功 → 全部传完
    - 部分成功 → 有些文件失败，会列出原因（不稳定、网络断等），可以点 重试
    - 空 → 远端目录下没有 allowlist 里的文件
    - 同时显示 传输 N 个文件，忽略 M 个（被忽略的是临时文件、缓存等）

3. 同步成功后，候选图区域会出现你保存的每张图。  


---

第 5 步：给候选图打分

每张候选图是一张卡片，包含：

- 图片预览（点开可看大图——目前直接显示，没单独大图视图）
- 提取的元数据（可展开）——PNG 里嵌的 prompt/workflow/model/seed 等，没提取到的字段显示"未知"
- 结果 下拉：成功 / 部分成功 / 失败
- 总分（1-10）
- 维度（来自评分模板，每个 1-10）
- 失败标签（多选）
- 备注（自由文本）
- 标记为已完成（不勾就是"草稿"，可以之后再补）  


操作流程：

1. 选结果 → 填分 → 选标签 → 写备注 → 点 保存评分
2. 卡片底部会显示 来源：人工 · 更新于 ...  


每张候选图独立打分，可以来回切换改。

---

第 6 步：调整评分模板（可选）

导航 → 评分模板：

- 当前模板：默认有 5 个维度（与目标对齐度、细节质量、连贯性、风格匹配、提示词遵循）+ 8  
  个失败标签（错误主体、肢体异常、模糊、颜色错误、构图差、伪影、过度平滑、其他）。
- 每个维度可以 启用/禁用。
- 下面 创建新版本 可以加/改维度和标签，保存为新版本 会版本号  
  +1。旧评分保留原版本标签，新评分用新模板。  


---

第 7 步：请求 AI 分析（可选，需要先配 AI 服务商）

1. 导航 → AI 服务商：


    - 类型：openai_compatible
    - Base URL：例如 OpenAI 是 https://api.openai.com/v1，OpenRouter 是

https://openrouter.ai/api/v1，Ollama 本地是 http://localhost:11434/v1  
 - 模型：gpt-4o / claude-3-5-sonnet / llava 等  
 - API Key：粘你的 key  
 - 保存  
 2. 回到实验详情页 → AI 分析历史 区域：  
 - 输入要分析的候选图 ID（目前 UI 没有可视化勾选，是输入框；候选图 ID 可以从详情页 URL  
 或候选卡片上找。改进在路线图上）  
 - 可选：勾 包含对比上下文（会带上同实验其它已确认评分给模型参考）  
 - 可选：目标覆盖（覆盖实验原本的目标描述）  
 - 点 请求 AI 分析  
 3. 等几秒（取决于服务商），分析条目会出现：  
 - 建议的失败原因（每个可勾选保留/取消）  
 - 建议的总分 + 结果  
 - 建议的备注  
 - 你可以编辑这些值，然后：  
 - 确认（编辑后） → 记为用户确认，原始 AI 建议保留作为溯源  
 - 全部拒绝 → 拒绝整条分析

---

没配 AutoDL 也能做的事  
 ┌────────────────────┬───────────────────────────────┐  
 │ 功能 │ 能用？ │  
 ├────────────────────┼───────────────────────────────┤  
 │ 创建实验、上传原图 │ 能 │  
 ├────────────────────┼───────────────────────────────┤  
 │ 看实验列表 / 详情 │ 能 │  
 ├────────────────────┼───────────────────────────────┤  
 │ 同步 │ 不能（必须有连接） │  
 ├────────────────────┼───────────────────────────────┤  
 │ 候选图评分 │ 能（但没候选图） │  
 ├────────────────────┼───────────────────────────────┤  
 │ 调整评分模板 │ 能 │  
 ├────────────────────┼───────────────────────────────┤  
 │ AI 分析 │ 需要 AI 服务商但不需要 AutoDL │  
 └────────────────────┴───────────────────────────────┘

---

关键设计点（避免踩坑）

- 私钥、API Key 永远不离开本地：存在 data/secrets/ 下，文件权限  
  0600，不会进日志，不会同步出去。
- 同步是手动触发的：平台不会后台轮询 AutoDL，不会在你不知情时拉文件。
- 同步不会调 AI：只有你显式点"请求 AI 分析"才会调服务商。
- 删除实验只删本地：远端 AutoDL 上的目录原封不动。
- 每张候选图保留所有历史快照：不会覆盖，每次同步都是新快照。
- 元数据未识别时显示"未知"：不会瞎猜。  


---

关停服务

# 终端

pkill -f "uvicorn app.main"  
 pkill -f "vite"

或者告诉我"停服"，我帮你 kill 后台进程。

---

我建议你先做这个最小闭环

没 AutoDL 也能跑：

1. 新建实验（任意一张图）→ 看 ID
2. 评分模板 → 看默认维度
3. AI 服务商 → 配一个（OpenRouter / Ollama 都行，免费可试）
4. 手动 mock 一个候选图：  
   mkdir -p data/artifacts/<实验ID>/snapshots/mock/outputs

# 复制一张图进去，重命名为 ComfyUI*00001*.png

4. 然后用 SQLite 直接插一条记录（或者跳过这步）
